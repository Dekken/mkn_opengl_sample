

name: mkn_opengl_sample
version: master
properties:
    git: http://github.com/mkn

dependencies:
  - name: gl.glew
    version: master
    scm: ${git}/gl.glew.git
  - name: gl.glfw
    version: master
    scm: ${git}/gl.glfw.git

main: main.c
